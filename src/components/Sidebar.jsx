import { Box } from "@mui/material";

export default function Sidebar() {
    return(
        <Box sx={{display: 'flex', width: '15vw', height: '100%', backgroundColor: 'blue'}}>
            Sidebar
        </Box>
    )
}